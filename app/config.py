SECRET_KEY = "secret key"

# DEBUG = False
# https://msiz07-flask-docs-ja.readthedocs.io/ja/latest/config.html#environment-and-debug-features
# 上記のように環境変数を使用することを推奨します。ENVとDEBUGをconfigまたはコードの中で設定することは可能ですが、そのようにはしないことを強く勧めます。configまたはコードの中で設定した値はflaskコマンドでは早い段階で読み取ることができず、そしていくつかのシステムまたはFlask拡張は、それらが読み取られるより前の値で既に設定を済ませてしまっているかもしれません。
